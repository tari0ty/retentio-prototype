// Copy this file to config.js and fill in your Supabase project values.
// Dashboard: Project Settings → API
window.RETENTIOOO_CONFIG = {
  // Project URL only — NOT the /rest/v1/ link from the API docs
  supabaseUrl: "https://YOUR_PROJECT_REF.supabase.co",
  // Settings → API → anon / publishable (public) key
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY",
  // Only this Google account may upload ad videos
  adminEmail: "your-google-email@example.com",
};
